import dotenv from 'dotenv';
dotenv.config();

// console.log('Email:', process.env.EMAIL);
// console.log('Password length:', process.env.EMAIL_PASSWORD ? process.env.EMAIL_PASSWORD.length : 0);
// console.log('First few chars of password:', process.env.EMAIL_PASSWORD ? process.env.EMAIL_PASSWORD.substring(0, 3) + '...' : 'none');